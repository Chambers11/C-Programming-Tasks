using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MathfSqrt : MonoBehaviour
{

    public TMPro.TMP_Text text_display;


    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
       // text_display.text = (Mathf.Sqrt(2.0f) * Mathf.Sqrt(2.0f)) == (2.0f).ToString(); // == cannot be applied to operands of type float and string 

        // Not Sure how to lay out line  20 but the out put value should equal true as 2 == 2  



}
}